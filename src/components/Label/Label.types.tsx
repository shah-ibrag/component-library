export interface LabelProps {
    text: string; // The text to be displayed in the label
    htmlFor?: string; // The id of the input element this label is associated with
    fontSize?: string; // Optional prop to define the font size
    color?: string; // Optional prop to define the color of the text
  }
  